# Cloudflare Workers (wrangler CLI). Do not commit real values.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# Optional GitHub token for raw dictionary reads from this repository.
GITHUB_TOKEN=

# Shared with Open Knowledge for server-to-server key issuance (Worker secret).
KEYS_ISSUE_SECRET=
